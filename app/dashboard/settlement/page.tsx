import { SettlementHubPage } from "@/modules/dashboard/settlement";

export const metadata = {
  title: "Settlement Hub | PDFBridge Infrastructure",
  description: "Deterministic settlement gateway for institutional invoice payouts.",
};

export default function Settlement() {
  return <SettlementHubPage />;
}
